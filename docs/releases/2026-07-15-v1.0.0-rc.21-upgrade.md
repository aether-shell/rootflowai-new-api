# 2026-07-15 v1.0.0-rc.21 Release

## Summary

- Upstream: `QuantumNous/new-api` tag `v1.0.0-rc.21`
- Upstream commit: `bde9b2f4`
- Local branch: `main`
- Local commit: `78421029313b0bf9b29e76b5dbf18cd7e71c0843`
- Previous image: `docker.io/rootflowai/new-api:v1.0.0-rc.20-rootflowai-20260710-21b7ddfd`
- Previous digest: `sha256:44a4edd01c549267e651b80c2db7e78a86b19bc94194d51cf7c652555f19f361`
- New image: `docker.io/rootflowai/new-api:v1.0.0-rc.21-rootflowai-20260715-78421029`
- New digest: `sha256:60ffc3aed58ccddcdc684d1496a33b2175081118b974601ed651ec2b2696c191`
- Platform: `linux/amd64`
- Archive: `/private/tmp/rootflowai-new-api-v1.0.0-rc.21-rootflowai-20260715-78421029.tar` (73 MiB)
- Archive SHA256: `e22f61aab32596eb8a5c5c90b1f41be1eb78a55364e653b4f55aec4bd0728793`
- Nodes imported: `dmit-server-1`, `dmit-server-2`, `dmit-server-3`
- DB changes: no new rc.21 production schema migration was required
- Canary mode: isolated Deployment with no Service and a non-production `app` label
- Rollback image: `docker.io/rootflowai/new-api:v1.0.0-rc.20-rootflowai-20260710-21b7ddfd`

## Included Changes

- Integrated all upstream `v1.0.0-rc.21` commits.
- Preserved RootFlowAI GPT-5.6 cache-write token accounting and explicit cache-write pricing.
- Preserved the `1.0x` default cache-write ratio for older OpenAI models without an explicit write price.
- Preserved cache usage normalization, billing propagation, overlap clamping, failed-stream no-charge behavior, task billing, payment controls, and channel affinity customizations.

## Validation

- `go test ./...`: passed.
- Backend link build: passed.
- Default frontend typecheck and production build: passed.
- Classic frontend production build: passed.
- GPT-5.6 overlap billing sample (`3619/2921/3616/36`): quota `4884`, passed.
- Production schema preflight: required tables and columns exist; no rc.21 schema gap found.
- Image archive SHA256 matched on all three service nodes before import.
- No paid model smoke request was initiated for this release.

## Canary

- Deployment: `new-api-rc21-canary`
- Image: rc.21 production image listed above.
- Isolation: label `app=rootflowai-new-api-rc21-canary`; no Service created; production Service endpoints did not include the canary Pod IP.
- Initial scheduling: `dmit-server-1` rejected the 200m CPU request because allocated CPU requests were full. The canary alone was moved to `dmit-server-3`, which had sufficient requested-resource headroom.
- Ready at: `2026-07-15 22:20:45 CST`.
- Observation: 20 minutes 53 seconds with `1/1 Ready`, zero restarts, HTTP 200, and no schema, panic, fatal, or 5xx log matches.
- Cleanup: deleted after all three formal Pods were Ready.

## Production Result

- Master Ready at `2026-07-15 22:44:49 CST` on `dmit-server-1`.
- Slave 1 Ready at `2026-07-15 22:48:21 CST` on `dmit-server-2`.
- Slave 2 Ready at `2026-07-15 22:48:57 CST` on `dmit-server-3`.
- Final topology: master `1/1`, slave `2/2`, all three on rc.21 with zero restarts.
- High-risk observation: at least 20 minutes after the final slave became Ready; Pod watch had no state changes.
- Internal status: all three `:30000/api/status` checks and master `:30001/api/status` returned HTTP 200.
- Public status: `api`, `admin`, `hk`, and `us` `/api/status` endpoints returned HTTP 200 in both immediate and final checks.
- DB heartbeat: all three rc.21 instances reported current heartbeats.
- DB read path: current actual Replica is `postgres-cluster-1`; final observed replay lag was about 5.35 seconds.

## Observed Errors

- Three GPT-5.5 requests returned 502 in one short window. Context showed upstream temporary failures across channels `#254` and `#275`; retries were attempted and precharges were returned. This was classified as an upstream incident, not an rc.21 application failure.
- One request returned 503 because its requested model had no available channel in the selected group. This was classified as channel availability, not an rc.21 application failure.
- The final 10-minute master scan had no severe errors or 5xx responses. The final slave scan had no schema, panic, or fatal errors.

## Rollback

- Rollback image is present on all three service nodes.
- No incompatible rc.21 schema change was applied.
- Rollback order, if required:

```bash
kubectl set image deployment/new-api-slave new-api-slave=docker.io/rootflowai/new-api:v1.0.0-rc.20-rootflowai-20260710-21b7ddfd -n airelay
kubectl rollout status deployment/new-api-slave -n airelay --timeout=300s
kubectl set image deployment/new-api-master new-api=docker.io/rootflowai/new-api:v1.0.0-rc.20-rootflowai-20260710-21b7ddfd -n airelay
kubectl rollout status deployment/new-api-master -n airelay --timeout=300s
```
